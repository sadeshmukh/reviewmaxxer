[project]
name = "reviewmaxxer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.13.5",
    "python-dotenv==1.0.0",
    "slack-bolt>=1.28.0",
]
